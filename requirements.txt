# FraPP + G.O.D.A.I. Dependencies
# Last audited: 2026-04-14 | See LICENSES.md for license details

# ── FraPP / API ──────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
sqlmodel>=0.0.14
pydantic-settings>=2.0.0
typer>=0.9.0

# ── G.O.D.A.I. ───────────────────────────────────────────────
pyyaml>=6.0

# ── ContextOS ────────────────────────────────────────────────
networkx>=3.0

# ── Anthropic SDK (required for AnthropicProvider) ───────────
anthropic>=0.40.0

# ── Local / offline LLM (optional) ───────────────────────────
# OllamaProvider uses httpx (already listed below) — no extra deps.
# LlamaCppProvider requires llama-cpp-python (CPU/GPU):
#   pip install llama-cpp-python
#   CMAKE_ARGS="-DLLAMA_CUDA=on" pip install llama-cpp-python   # GPU
# Install Ollama runtime: bash scripts/install_local_llm.sh

# ── Testing ──────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
